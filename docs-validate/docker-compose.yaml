services:
  docs-validator:
    build:
      context: ..
      dockerfile: docs-validate/Dockerfile
    image: <MODULE-NAME>-docs-validate:latest
    working_dir: /workspace
    volumes:
      - ${HOST_PROJECT_PATH:-..}:/workspace
    command: mkdocs build --strict
